<%= link_to "New List", new_list_path, class: "btn btn-primary" %>
<div class="container mt-3">
  <div class="row g-3">
    <% @lists.each do |list| %>
      <div class="card col-md-4 mt-3 shadow-lg" >
        <%= image_tag "https://source.unsplash.com/random/300x200/?#{list.name}", alt: "Image for #{list.name}" %>
        <div class="card-img-overlay d-flex flex-column align-items-center justify-content-center">
          <div class="card-body d-flex flex-column align-items-center justify-content-center text-center">
            <h3 style="color: white; text-shadow: 2px 2px 2px black"><%= list.name %></h3>
          </div>
        </div>
        <%= link_to '', list_path(list), class: 'card-trip-link' %>
      </div>
    <% end %>
  </div>
</div>
